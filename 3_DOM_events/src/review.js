// Array manipulation
arr = [1,2,3,4]
fruits = ["apple","orange"]
/*
arr.push(5)
arr.unshift(0)
arr.pop()
arr.shift()
arrCopy = [...]
arr.slice(-2)
arr.splice(1,2)

// Map Function

const mapReturn = arr.map((item)=>{
    console.log(item)
    return item * 10
}) 

console.log(mapReturn)

// forEach Function

arr.forEach((item) => {
    console.log(item)
});

// Query Selectors

console.log(document.querySelector("div main"))
const vari = document.querySelector("#id")
document.querySelector(".class")
*/

/*

const header = document.querySelector("h1")
header.textContent = "This is a Header"
header.id = "Unique Id"

const headerDiv = document.querySelector("div header div")

const img = document.createElement("img")
headerDiv.append(img)

*/
